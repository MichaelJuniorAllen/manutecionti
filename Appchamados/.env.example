# Backend
PORT=4000
JWT_SECRET=chamados-secret-dev
JWT_EXPIRES_IN=8h
CORS_ORIGINS=http://localhost:5173,https://seu-site.netlify.app,https://*.netlify.app
DATABASE_URL=postgresql://USER:PASS@HOST:5432/DBNAME
PGSSL=true

# SMTP (envio real de codigo de confirmacao de e-mail)
SMTP_HOST=smtp.seu-provedor.com
SMTP_PORT=587
SMTP_USER=seu_usuario
SMTP_PASS=sua_senha
SMTP_FROM=Manutecao TI <no-reply@seudominio.com>

# Frontend (opcional)
VITE_API_URL=http://localhost:4000/api
VITE_SERVER_URL=http://localhost:4000
